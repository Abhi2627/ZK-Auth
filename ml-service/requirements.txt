# ZK-Auth ML Inference Service — Python Dependencies
# Pin minor versions for reproducibility; update deliberately.

# ─── gRPC ─────────────────────────────────────────────────────────────────────
grpcio==1.60.0              # Older version compatible with protobuf 4.x
grpcio-tools==1.60.0
grpcio-health-checking==1.60.0
protobuf>=3.20.3,<5.0.0     # TensorFlow 2.16.2 requires protobuf < 5.0.0

# ─── ML Framework ─────────────────────────────────────────────────────────────
tensorflow==2.16.2          # includes Keras 3
numpy==1.26.4
scikit-learn==1.5.0         # StandardScaler, joblib serialisation
joblib==1.4.2               # scaler persistence

# ─── Database (TimescaleDB reads for training pipeline) ───────────────────────
psycopg2-binary==2.9.9
sqlalchemy==2.0.31

# ─── Configuration & Utilities ───────────────────────────────────────────────
python-dotenv==1.0.1
pydantic==2.7.4             # config validation (mirrors Zod pattern from backend)
pydantic-settings==2.3.4

# ─── Observability ────────────────────────────────────────────────────────────
structlog==24.2.0           # structured logging (mirrors pino pattern)
prometheus-client==0.20.0   # /metrics endpoint for future scraping

# ─── Testing ──────────────────────────────────────────────────────────────────
pytest==8.2.2
pytest-asyncio==0.23.7
