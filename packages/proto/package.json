{
  "name": "@zk-auth/proto",
  "version": "1.0.0",
  "description": "Protobuf definitions for ZK-Auth gRPC services",
  "private": true,
  "scripts": {
    "generate": "echo 'Run: cd backend && npm run proto:install  (installs grpc-tools on demand)'"
  }
}
