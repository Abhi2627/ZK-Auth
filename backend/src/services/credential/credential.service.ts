/**
 * Credential Service — Phase 3 target
 * Will implement: credential issuance, Merkle tree construction,
 * status management, and revocation.
 */
export class CredentialService {
  // TODO Phase 3: issueCredential(), revokeCredential(), getCredential()
}
