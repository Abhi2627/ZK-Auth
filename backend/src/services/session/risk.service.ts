/**
 * Risk Service — Phase 3 target
 * Will implement: EMA risk score smoothing, threshold classification,
 * step-up event creation, Redis risk cache reads/writes.
 */
export class RiskService {
  // TODO Phase 3: updateRiskScore(), getRiskScore(), triggerStepUp(), resolveStepUp()
}
