/**
 * Telemetry Service — Phase 3 target
 * Will implement: batched INSERT to TimescaleDB behavior_events hypertable,
 * sequence gap detection, and write-path back-pressure handling.
 */
export class TelemetryService {
  // TODO Phase 3: insertEvents(), detectSequenceGaps(), flushBatch()
}
